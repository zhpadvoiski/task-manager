tasks.register("buildAll"){
    group "build"
    dependsOn ":client:build", ":server:build"
}

tasks.register("runDev"){
    group "application"
    dependsOn tasks.buildAll

    finalizedBy(":server:runDev")
}

