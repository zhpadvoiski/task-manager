tasks.register("buildAll"){
    group "build"
    dependsOn ":client:build"
    dependsOn ":server:build"
}

tasks.register("runDev"){
    group "application"
    dependsOn tasks.buildAll
    dependsOn ":server:runDev"
}

